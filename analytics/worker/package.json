{
  "name": "eileenip-analytics",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute eileenip-analytics --remote --file=./schema.sql",
    "schema:local": "wrangler d1 execute eileenip-analytics --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
